import React from 'react'

const ShortCircuitEval = () => {
  return (
    <>
        <div className="main-useState-Sce">
        <h2>Short Circuit Evaluation</h2>
        <p>Ok, A simple way to show or hide elements on a react application is implement the “short circuit condition” where the syntax is this: react.jsx.  condition && (Html or subcomponents to render)  So, the condition needs to be fulfilled in order to render the html or subcomponent.</p>
        </div>
    </>
  )
}

export default ShortCircuitEval